[Jetbrains_]

exten => s,1,Answer()
same  => n,NoOp(Home)
same  => n,WaitExten(7)

exten => t,1,NoOp(Timeout)
same  => n,Goto(JetbrainsTIMEOUT_,s,1)

exten => i,1,NoOp(Invalid Input)
same  => n,Goto(Jetbrains_,s,1)

exten => 2,1,Goto(Internet_a0_0,s,1)
;=================================================================
[Internet_a0_0]
exten => s,1,Answer()
same  => n,NoOp("Internet")
exten => t,1,NoOp(Timeout)
same  => n,Goto(,s,1)
exten => i,1,NoOp(Invalid Input)
same  => n,Goto(Internet_a0_0,s,1)

[JetbrainsTIMEOUT_]
;Timeout
exten => s,1,NoOp(Timeout)
same  => n,NoOp(ReplayOptions)
same  => n,Goto(Jetbrains_,s,1)
exten => i,1,Goto(JetbrainsTIMEOUT_,s,1)

;=================================================================  

[default]
exten => 800,1,Goto(Jetbrains_,s,1)

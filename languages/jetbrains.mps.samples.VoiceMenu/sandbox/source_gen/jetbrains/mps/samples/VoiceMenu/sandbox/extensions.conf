[MY TEST_]

exten => s,1,Answer()
same  => n,NoOp("Home")
same  => n,PlayBack()


exten => t,1,NoOp("timeout")
same  => n,Goto(MY TESTTIMEOUT_,s)

exten => i,n,NoOp("invalid")
same  => n,Goto(MY TEST_,s)

exten => 8,1,Goto(Action_37_a0,s)
exten => 4,1,Goto(gggggg_b0,s)
exten => 0,1,Goto(gsgds_c0,s)
[Action_37_a0]

exten => s,1,Playback(file.wav)
same  => n,NoOp("Action_37")
same  => n,NoOp("HangUp")
same  => n,HangUp()



[gggggg_b0]

exten => s,1,Playback(Choose from.wav)
same  => n,NoOp("gggggg")
same  => n,NoOp("Other")
same  => n,Goto(MY TEST_,s)



[gsgds_c0]

exten => s,1,Playback()
same  => n,NoOp("gsgds")
same  => n,NoOp("Other")
same  => n,HangUp()







[MY TESTTIMEOUT_]
exten => s,1,NoOp("timeout")


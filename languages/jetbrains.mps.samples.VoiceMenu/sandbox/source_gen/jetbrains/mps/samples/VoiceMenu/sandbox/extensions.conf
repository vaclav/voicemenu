[JetBrains_]
exten => s,1,PlayBack()
same  => n,NoOp("Home")
same  => n,PlayBack(Welcome to the best company ever)


exten => t,1,NoOp("timeout")
same  => n,Goto(,s)

exten => i,n,NoOp("invalid")
same  => n,Goto(JetBrains_,s)

exten => 1,1,Goto(Sales_a0_0,s)


[Sales_a0_0]

exten => s,1,Answer()
same  => n,Playback()
exten => t,1,NoOp("timeout")
same  => n,GoTo(SalesTIMEOUT_a0_0,s)

exten => i,1,NoOp("Invalid Input")
same  => n,GoTo(Sales_a0_0,s)

exten => 1,1,GoTo(Menu_a0a0, s)


[Menu_a0a0]

exten => s,1,Answer()
same  => n,Playback()
exten => t,1,NoOp("timeout")
same  => n,GoTo(MenuTIMEOUT_a0a0_0,s)

exten => i,1,NoOp("Invalid Input")
same  => n,GoTo(Menu_a0a0,s)

exten => 1,1,GoTo(Call_a0a0a0, s)


[Call_a0a0a0]

exten => s,1,Answer()
same  => n,Playback()
same  => n,NoOp("Direct Call")
same  => n,Dial(,15)
same  => n,HangUp()



[CallTIMEOUT_a0a0a0]
exten => s,1,NoOp("timeout")
same  => n,NoOp("Direct Call")
same  => n,Dial(,15)
same  => n,HangUp()


[MenuTIMEOUT_a0a0]
exten => s,1,NoOp("timeout")
same  => n,NoOp("Direct Call")
same  => n,Dial(,15)
same  => n,HangUp()


[SalesTIMEOUT_a0_0]
exten => s,1,NoOp("timeout")
same  => n,NoOp("Direct Call")
same  => n,Dial(,15)
same  => n,HangUp()


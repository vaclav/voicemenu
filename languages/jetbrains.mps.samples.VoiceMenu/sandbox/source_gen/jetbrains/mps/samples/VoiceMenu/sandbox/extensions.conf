[JetBrains_]

exten => s,1,Answer()
same  => n,NoOp("Home")
same  => n,PlayBack(Welcome to the best company ever)


exten => t,1,NoOp("timeout")
same  => n,Goto(JetBrainsTIMEOUT_,s)

exten => i,n,NoOp("invalid")
same  => n,Goto(JetBrains_,s)

exten => 1,1,Goto(Menu1_a0,s)
[Menu1_a0]

exten => s,1,Playback()
same  => n,NoOp("Menu1")
exten => t,1,NoOp("timeout")
same  => n,GoTo(Menu1TIMEOUT_a0,s)

exten => i,1,NoOp("Invalid Input")
same  => n,GoTo(Menu1_a0,s)

exten => 1,1,GoTo(Menu2_a0a0, s)

[Menu2_a0a0]

exten => s,1,Playback()
same  => n,NoOp("Menu2")
exten => t,1,NoOp("timeout")
same  => n,GoTo(Menu2TIMEOUT_a0a0_0,s)

exten => i,1,NoOp("Invalid Input")
same  => n,GoTo(Menu2_a0a0,s)

exten => 1,1,GoTo(Menu3_a0a0a0, s)

[Menu3_a0a0a0]

exten => s,1,Playback()
same  => n,NoOp("Menu3")
exten => t,1,NoOp("timeout")
same  => n,GoTo(Menu3TIMEOUT_a0a0a0_2,s)

exten => i,1,NoOp("Invalid Input")
same  => n,GoTo(Menu3_a0a0a0,s)

exten => 1,1,GoTo(Call_a0a0a0a0, s)

[Call_a0a0a0a0]

exten => s,1,Playback()
same  => n,NoOp("Call")
same  => n,NoOp("Direct Call")
same  => n,Dial(,15)
same  => n,HangUp()




[Menu3TIMEOUT_a0a0a0]
exten => s,1,NoOp("timeout")
same  => n,NoOp("ReplayOptions")
same  => n,Goto(Menu3_a0a0a0,s)

[Menu2TIMEOUT_a0a0]
exten => s,1,NoOp("timeout")
same  => n,NoOp("ReplayOptions")
same  => n,Goto(Menu2_a0a0,s)

[Menu1TIMEOUT_a0]
exten => s,1,NoOp("timeout")
same  => n,NoOp("ReplayOptions")
same  => n,Goto(Menu1_a0,s)




[JetBrainsTIMEOUT_]
exten => s,1,NoOp("timeout")
same  => n,NoOp("ReplayOptions")
same  => n,Goto(JetBrains_,s)


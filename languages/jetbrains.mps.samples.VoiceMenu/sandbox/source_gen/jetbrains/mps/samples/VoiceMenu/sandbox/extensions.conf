[myVoiceMenu_]
exten => s,1,PlayBack()
same  => n,NoOp("Home")

exten => 1,1,Goto(Internet_a0_0,s)
exten => 2,1,Goto(Television_b0,s)


[Internet_a0_0]

exten => s,1,Answer()
same  => n,Playback()
same  => n,WaitExten(5)
exten => t,1,NoOp("timeout")
same  => n,GoTo(Internet_a0_0,s)

exten => i,1,NoOp("Invalid Input")
same  => n,GoTo(Internet_a0_0,s)

exten => 1,1,GoTo(Speed_up_a0a0, s)
exten => 2,1,GoTo(Discount_b0a0, s)


[Speed_up_a0a0]

exten => s,1,Answer()
same  => n,Playback()
same  => n,WaitExten(5)
same  => n,NoOp("Get Info")
same  => n,PlayBack()
same  => n,Goto(Internet_a0_0,s)
exten => t,1,NoOp("timeout")
same  => n,GoTo(Speed_up_a0a0,s)

exten => i,1,NoOp("Invalid Input")
same  => n,GoTo(Speed_up_a0a0,s)



[Discount_b0a0]

exten => s,1,Answer()
same  => n,Playback()
same  => n,WaitExten(5)
exten => t,1,NoOp("timeout")
same  => n,GoTo(Discount_b0a0,s)

exten => i,1,NoOp("Invalid Input")
same  => n,GoTo(Discount_b0a0,s)

exten => 2,1,GoTo(Small_Discount_a0b0a0, s)
exten => 1,1,GoTo(Big_Discount_b0b0a0, s)
exten => *,1,GoTo(Going_Back_c0b0a0, s)
exten => #,1,GoTo(Going_Back_d0b0a0, s)


[Big_Discount_b0b0a0]

exten => s,1,Answer()
same  => n,Playback()
same  => n,WaitExten(5)
same  => n,NoOp("Direct Call")
same  => n,Dial(,15)
same  => n,HangUp()
exten => t,1,NoOp("timeout")
same  => n,GoTo(Big_Discount_b0b0a0,s)

exten => i,1,NoOp("Invalid Input")
same  => n,GoTo(Big_Discount_b0b0a0,s)



[Small_Discount_a0b0a0]

exten => s,1,Answer()
same  => n,Playback()
same  => n,WaitExten(5)
same  => n,NoOp("Direct Call")
same  => n,Dial(,15)
same  => n,Goto(Discount_b0a0,s)
exten => t,1,NoOp("timeout")
same  => n,GoTo(Small_Discount_a0b0a0,s)

exten => i,1,NoOp("Invalid Input")
same  => n,GoTo(Small_Discount_a0b0a0,s)



[Going_Back_d0b0a0]

exten => s,1,Answer()
same  => n,Playback()
same  => n,WaitExten(5)
same  => n,NoOp("Back")
same  => n,Goto(Internet_a0_0,s)
exten => t,1,NoOp("timeout")
same  => n,GoTo(Going_Back_d0b0a0,s)

exten => i,1,NoOp("Invalid Input")
same  => n,GoTo(Going_Back_d0b0a0,s)



[Going_Back_c0b0a0]

exten => s,1,Answer()
same  => n,Playback()
same  => n,WaitExten(5)
same  => n,NoOp("Back")
same  => n,Goto(Internet_a0_0,s)
exten => t,1,NoOp("timeout")
same  => n,GoTo(Going_Back_c0b0a0,s)

exten => i,1,NoOp("Invalid Input")
same  => n,GoTo(Going_Back_c0b0a0,s)



[Television_b0]

exten => s,1,Answer()
same  => n,Playback()
same  => n,WaitExten(5)
same  => n,NoOp("Other")
same  => n,HangUp()
exten => t,1,NoOp("timeout")
same  => n,GoTo(Television_b0,s)

exten => i,1,NoOp("Invalid Input")
same  => n,GoTo(Television_b0,s)


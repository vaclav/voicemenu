[Test160_]

exten => s,1,Answer()
same  => n,NoOp("Home")
same  => n,PlayBack()


exten => t,1,NoOp("timeout")
same  => n,Goto(Test160TIMEOUT_,s)

exten => i,n,NoOp("invalid")
same  => n,Goto(Test160_,s)

exten => 3,1,Goto(File_b0,s)
exten => 2,1,Goto(Text_a0,s)
[File_b0]

exten => s,1,Playback(jk;sample.wav)
same  => n,NoOp("File")
same  => n,NoOp("Get Info")
same  => n,HangUp()



[Text_a0]

exten => s,1,Playback(Choose from)
same  => n,NoOp("Text")
same  => n,NoOp("Get Info")
same  => n,HangUp()







[Test160TIMEOUT_]
exten => s,1,NoOp("timeout")
same  => n,GoTo(,s)

